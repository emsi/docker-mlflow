To run as current user run following script once  to set proper values in .env file:</br>
`./run_as_user.sh`

To run `mlflow ui`, go to direcotry with `mlruns` and type:</br>
`docker compose --project-directory <path_to/docker-mlflow> up -d`
